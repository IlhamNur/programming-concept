/* C Program to convert temperature from Fahrenheit to Celsius */
#include <stdio.h>
 
void celsiusToFahrenheit (double* x); //argument "var" is given in Celcius
void fahrenheitToCelcius (double* x); //argument "var" is given in Fahrenheit

int main(void)
{

    double x, f, c;
 
    scanf("%d",&x);
 
      void ctof(double *x) {
		 *x = 9.0/5.0 * (*x) + 32;
		}	
        printf("Temperature in Celsius: %.2f\n",c);
        
  		void ctof(double *x) {
		 *x = 5.0/9.0 * ((*x) - 32);
		}
        printf("Temperature in Fahrenheit: %.2f",f);
   
    
    return 0;
}

/* maaf jika pengerjaan saya tidak teratur dan atau bahkan tidak menemukan solusinya. 
	Saya berusaha mengejar ketertinggalan saya dari materi yang diberikan tapi belum bisa. 
	mohon dimaklumi karena pemrograman adalah hal yang baru bagi saya */

